{
  "name": "collegeai-root",
  "version": "1.0.0",
  "description": "CollegeAI RAG Full-stack Workspace",
  "scripts": {
    "dev": "concurrently -n \"SERVER,CLIENT\" -c \"indigo.bold,cyan.bold\" \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "start": "concurrently -n \"SERVER,CLIENT\" -c \"indigo.bold,cyan.bold\" \"npm run start --prefix server\" \"npm run start --prefix client\"",
    "install:all": "npm install --prefix server && npm install --prefix client",
    "test": "npm test --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
